Revison history.

Todo:
 - Make a do not include this tag in main index flag
	- or -
 - The inverse so we can segregate solutions from the news.
 - Probably should allow for both.
 
 - Move to using django-tagging to do the tags.
 - Add options entries for how long comments are open.
 
1.0.1-rc1
 - Use "permalink(get_absolute_url)" rather than "@models.permalink",
   for python 2.3
 - Enable comments feed via the comments contrib model.
 - Template tag, "tag_list_as" to list all tags.
 - Template tag, "entry_archive" to list entry dates
